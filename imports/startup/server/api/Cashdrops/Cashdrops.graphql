

type userdrop {
    name:String
    amDrop:Int
    pmDrop:Int
    totalDrop:Int
    # id:ID
}

extend type Mutation {
    createCashdrop(totalDrop:Int,amDrop:Int,pmDrop:Int,shiftStart:Int,shiftEnd:Int,userDrop:Int,id:ID,name:String):userdrop
}
